{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"cd client && npm run build ",
    "install_client":"cd client && npm install ",

    "heroku-postbuild":"npm run install_client && npm run build ",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^12.0.3",
    "express": "^4.17.2",
    "mongoose": "^6.1.6"
  }
}
